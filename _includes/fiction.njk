<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="utf-8" />
  <title>{{ title }}</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
    /* —— 场景 BGM 同意卡片 —— */
    .bgm-consent {
      margin: 10px 0 24px;
      padding: 14px 16px;
      border-radius: 16px;
      background: radial-gradient(circle at top left,
        rgba(148, 163, 253, 0.18),
        rgba(15, 23, 42, 0.96)
      );
      border: 1px solid rgba(129, 140, 248, 0.5);
      max-width: 720px;
      font-size: 14px;
      color: #e5e7eb;
    }

    .bgm-consent-text {
      margin: 0 0 10px;
      line-height: 1.7;
    }

    .bgm-consent-btn {
      border: none;
      border-radius: 999px;
      padding: 6px 14px;
      font-size: 13px;
      cursor: pointer;
      background: rgba(129, 140, 248, 0.95);
      color: #0b1020;
      font-weight: 500;
      display: inline-flex;
      align-items: center;
      gap: 6px;
      box-shadow: 0 8px 24px rgba(15, 23, 42, 0.8);
    }

    .bgm-consent-btn:hover {
      background: rgba(99, 102, 241, 1);
    }

    .bgm-consent.hidden {
      display: none;
    }
  <style>
    :root {
      color-scheme: dark;
    }

    * {
      box-sizing: border-box;
    }

    /* —— 通用背景：云光 + 夜色 —— */
    body {
      margin: 0;
      font-family: "Noto Serif SC", "Source Han Serif SC", "Songti SC",
        -apple-system, BlinkMacSystemFont, system-ui, "SF Pro Text", "Segoe UI",
        sans-serif;
      background:
        radial-gradient(circle at 20% 0%, rgba(129, 140, 248, 0.24) 0, transparent 55%),
        radial-gradient(circle at 80% 100%, rgba(56, 189, 248, 0.18) 0, transparent 60%),
        radial-gradient(circle at top, #111827 0, #020617 55%);
      color: #f5f5f5;
    }

    /* —— 页面淡入动画 —— */
    .page-fade {
      animation: pageFadeIn 650ms ease-out both;
    }

    @keyframes pageFadeIn {
      from {
        opacity: 0;
        transform: translateY(8px);
        filter: blur(2px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
        filter: blur(0);
      }
    }

    .fiction-page {
      min-height: 100vh;
      max-width: 880px;
      margin: 0 auto;
      padding: 32px 20px 64px;
    }

    .fiction-header {
      margin-bottom: 18px;
    }

    .fiction-back {
      font-size: 14px;
      color: #aab3ff;
      text-decoration: none;
    }

    .fiction-back:hover {
      text-decoration: underline;
    }

    .fiction-title {
      margin: 10px 0 6px;
      font-size: 30px;
      font-weight: 650;
      letter-spacing: 0.03em;
    }

    .fiction-meta {
      font-size: 13px;
      color: #9ca3af;
    }

    .fiction-hero {
      margin: 26px 0 18px;
      border-radius: 18px;
      overflow: hidden;
      box-shadow: 0 22px 40px rgba(0, 0, 0, 0.55);
    }

    .fiction-hero img {
      display: block;
      width: 100%;
      height: auto;
    }

    /* —— 正文排版 —— */
    .fiction-body {
      margin-top: 24px;
      max-width: 720px;
      font-family:
        "Songti SC",
        "STSong",
        "SimSun",
        "Noto Serif CJK SC",
        "Source Han Serif SC",
        "Times New Roman",
        serif;
      font-size: 17px;
      line-height: 1.9;
      letter-spacing: 0.02em;
    }

    .fiction-body p {
      margin: 0 0 1.2em;
      text-indent: 2em;
    }

    .fiction-body p:last-child {
      margin-bottom: 0;
    }

    .fiction-body ul,
    .fiction-body ol,
    .fiction-body blockquote {
      margin: 0 0 1.2em 0;
      padding-left: 2em;
      text-indent: 0;
    }

    /* —— 场景块组件（和 js/scene-audio.js 对应） —— */

    /* 整个场景区域：class="story-scene" data-audio="..." */
    .story-scene {
      margin-top: 32px;
      margin-bottom: 32px;
      padding: 20px;
      border-radius: 20px;
      background: radial-gradient(
        circle at top left,
        rgba(148, 163, 253, 0.16),
        rgba(15, 23, 42, 0.95)
      );
      display: grid;
      grid-template-columns: minmax(0, 1.2fr) minmax(0, 2fr);
      gap: 20px;
      align-items: stretch;
      box-shadow: 0 18px 40px rgba(0, 0, 0, 0.55);
    }

    @media (max-width: 768px) {
      .story-scene {
        grid-template-columns: minmax(0, 1fr);
      }
    }

    .scene-media img {
      width: 100%;
      height: 100%;
      object-fit: cover;
      border-radius: 16px;
      display: block;
      background: #020617;
    }

    .scene-text {
      display: flex;
      flex-direction: column;
      gap: 12px;
    }

    .scene-label {
      display: inline-flex;
      align-self: flex-start;
      padding: 4px 10px;
      border-radius: 999px;
      font-size: 11px;
      letter-spacing: 0.12em;
      text-transform: uppercase;
      background: rgba(148, 163, 253, 0.18);
      color: #c7d2fe;
    }

    .scene-body {
      font-size: 15px;
      line-height: 1.8;
      color: #e5e7eb;
    }

    .scene-body p {
      margin: 0 0 0.7em;
      text-indent: 0;
    }

    .scene-body p:last-child {
      margin-bottom: 0;
    }

    .audio-indicator {
      margin-top: 6px;
      font-size: 12px;
      color: #a5b4fc;
      opacity: 0.9;
    }

    /* —— 整篇小说 BGM 块（可选，用于显示播放器的那种） —— */

    .fiction-audio {
      margin-top: 28px;
      max-width: 720px;
      padding: 14px 18px;
      border-radius: 16px;
      background: rgba(15, 23, 42, 0.9);
      display: flex;
      flex-direction: column;
      gap: 8px;
    }

    .fiction-audio-label {
      font-size: 14px;
      color: #cbd5f5;
    }

    audio {
      width: 100%;
    }
  </style>
</head>
<body>
  <main class="fiction-page page-fade">
    <header class="fiction-header">
      <a href="/fiction/" class="fiction-back">← 返回小说列表</a>
      <h1 class="fiction-title">{{ title }}</h1>
      <p class="fiction-meta">
        {{ page.date | date("yyyy-MM-dd") }}
      </p>
    </header>
    <!-- BGM 同意提示卡片 -->
    <section id="bgm-consent" class="bgm-consent">
      <p class="bgm-consent-text">
        本篇故事的某些段落会配有背景音乐。<br>
        点击下方按钮，同意在阅读过程中按场景自动播放 BGM。
      </p>
      <button id="bgmConsentBtn" class="bgm-consent-btn">
        🎧 开启场景背景音乐
      </button>
    </section>

    {% if cover %}
    <figure class="fiction-hero">
      <img src="{{ cover }}" alt="{{ title }} 封面图" />
    </figure>
    {% endif %}

    <section class="fiction-body">
      {{ content | safe }}
    </section>

    {% if audio %}
    <section class="fiction-audio">
      <div class="fiction-audio-label">背景音乐</div>
      <audio controls preload="none">
        <source src="{{ audio }}" type="audio/mpeg" />
        你的浏览器不支持音频播放。
      </audio>
    </section>
    {% endif %}
  </main>

  <!-- 加载自动场景 BGM 的脚本：匹配 .story-scene[data-audio] -->
  <script src="/js/scene-audio.js"></script>
</body>
</html>
