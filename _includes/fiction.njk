<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="utf-8">
  <title>{{ title }}</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <style>
    :root { color-scheme: dark; }

    * { box-sizing: border-box; }

    body {
      margin: 0;
      font-family: -apple-system, BlinkMacSystemFont, system-ui, "SF Pro Text",
        "Segoe UI", sans-serif;
      background: radial-gradient(circle at top, #111827 0, #020617 55%);
      color: #f9fafb;
    }

    .fiction-page {
      min-height: 100vh;
      max-width: 840px;
      margin: 0 auto;
      padding: 32px 18px 60px;
    }

    /* ---------- 头部 ---------- */

    .fiction-header {
      margin-bottom: 8px;
    }

    .fiction-back {
      font-size: 13px;
      color: #a5b4fc;
      text-decoration: none;
    }

    .fiction-back:hover {
      text-decoration: underline;
    }

    .fiction-title {
      margin: 10px 0 4px;
      font-size: 30px;
      font-weight: 650;
      letter-spacing: 0.02em;
    }

    .fiction-meta-row {
      display: flex;
      align-items: center;
      gap: 10px;
      margin-bottom: 4px;
      font-size: 13px;
      color: #9ca3af;
    }

    .fiction-chip {
      padding: 2px 10px;
      border-radius: 999px;
      border: 1px solid rgba(148, 163, 253, 0.55);
      font-size: 11px;
      letter-spacing: 0.14em;
      text-transform: uppercase;
      color: #c7d2fe;
    }

    .fiction-tagline {
      margin: 6px 0 14px;
      font-size: 14px;
      line-height: 1.7;
      color: #e5e7eb;
      opacity: 0.9;
    }

    /* ---------- 封面图 ---------- */

    .fiction-hero {
      margin: 18px 0 20px;
      border-radius: 18px;
      overflow: hidden;
      box-shadow: 0 22px 40px rgba(0, 0, 0, 0.7);
      background: #020617;
    }

    .fiction-hero img {
      display: block;
      width: 100%;
      height: auto;
    }

    /* ---------- 正文 ---------- */

    .fiction-body {
      margin-top: 4px;
      line-height: 1.9;
      font-size: 17px;
      letter-spacing: 0.02em;
      padding: 22px 22px 26px;
      border-radius: 20px;
      background: radial-gradient(circle at top left, rgba(30, 64, 175, 0.32), rgba(15, 23, 42, 0.96));
      backdrop-filter: blur(14px);
      box-shadow: 0 18px 40px rgba(0, 0, 0, 0.75);
    }

    .fiction-body p {
      margin: 0 0 1.1em;
      text-indent: 2em;   /* 首行缩进 */
    }

    .fiction-body p:last-child {
      margin-bottom: 0;
    }

    .fiction-body h2,
    .fiction-body h3,
    .fiction-body h4 {
      margin: 1.8em 0 0.8em;
      text-indent: 0;
    }

    /* ---------- 场景块组件 ---------- */

    .scene-block {
      margin: 30px 0;
      padding: 20px;
      border-radius: 22px;
      background: radial-gradient(circle at top left,
          rgba(129, 140, 248, 0.16),
          rgba(15, 23, 42, 0.96));
      display: grid;
      grid-template-columns: minmax(0, 1.4fr) minmax(0, 2fr);
      gap: 20px;
      align-items: stretch;
      box-shadow: 0 18px 40px rgba(0, 0, 0, 0.75);
      border: 1px solid rgba(148, 163, 253, 0.25);
    }

    @media (max-width: 768px) {
      .scene-block {
        grid-template-columns: minmax(0, 1fr);
      }
    }

    .scene-media {
      border-radius: 18px;
      overflow: hidden;
      background: #020617;
    }

    .scene-media img {
      width: 100%;
      height: 100%;
      object-fit: cover;
      display: block;
    }

    .scene-text {
      display: flex;
      flex-direction: column;
      gap: 12px;
    }

    .scene-tag {
      display: inline-flex;
      align-self: flex-start;
      padding: 4px 11px;
      border-radius: 999px;
      font-size: 11px;
      letter-spacing: 0.16em;
      text-transform: uppercase;
      background: rgba(129, 140, 248, 0.26);
      color: #e0e7ff;
    }

    .scene-body {
      font-size: 15px;
      line-height: 1.8;
      color: #e5e7eb;
    }

    .scene-body p {
      margin: 0 0 0.8em;
      text-indent: 0;   /* 场景里不缩进 */
    }

    .scene-audio {
      margin-top: 6px;
      padding: 9px 11px;
      border-radius: 14px;
      background: rgba(15, 23, 42, 0.96);
      border: 1px solid rgba(148, 163, 253, 0.18);
    }

    .scene-audio-label {
      font-size: 12px;
      color: #9ca3af;
      margin-bottom: 4px;
    }

    .scene-audio audio {
      width: 100%;
    }

    /* ---------- 底部 BGM ---------- */

    .fiction-audio {
      margin-top: 26px;
      padding: 14px 18px;
      border-radius: 16px;
      background: rgba(15, 23, 42, 0.9);
      display: flex;
      flex-direction: column;
      gap: 8px;
      box-shadow: 0 16px 30px rgba(0, 0, 0, 0.6);
    }

    .fiction-audio-label {
      font-size: 14px;
      color: #cbd5f5;
    }

    audio { width: 100%; }
  </style>
</head>
<body>
  <main class="fiction-page">
    <header class="fiction-header">
      <a href="/fiction/" class="fiction-back">← 返回小说列表</a>

      <h1 class="fiction-title">{{ title }}</h1>

      <div class="fiction-meta-row">
        <span class="fiction-chip">FICTION</span>
        <span>{{ page.date | date("yyyy-MM-dd") }}</span>
      </div>

      {# 使用 front matter 里的简介/摘要字段（excerpt）作为 tagline #}
      {% if excerpt %}
        <p class="fiction-tagline">{{ excerpt }}</p>
      {% endif %}
    </header>

    {# 封面图片：有 cover 就显示 #}
    {% if cover %}
      <figure class="fiction-hero">
        <img src="{{ cover }}" alt="{{ title }} 封面图">
      </figure>
    {% endif %}

    <section class="fiction-body">
      {{ content | safe }}
    </section>

    {# 底部背景音乐：有 audio 就渲染播放器 #}
    {% if audio %}
      <section class="fiction-audio">
        <div class="fiction-audio-label">背景音乐</div>
        <audio controls preload="none">
          <source src="{{ audio }}" type="audio/mpeg">
          你的浏览器不支持音频播放。
        </audio>
      </section>
    {% endif %}
  </main>
</body>
</html>
