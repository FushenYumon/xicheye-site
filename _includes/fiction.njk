<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="utf-8">
  <title>{{ title }}</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <style>
    :root { color-scheme: dark; }

    * { box-sizing: border-box; }

    body {
      margin: 0;
      font-family: -apple-system, BlinkMacSystemFont, system-ui, "SF Pro Text",
        "Segoe UI", sans-serif;
      background: radial-gradient(circle at top, #222b45 0, #050814 55%);
      color: #f5f5f5;
    }

    .fiction-page {
      min-height: 100vh;
      max-width: 760px;
      margin: 0 auto;
      padding: 32px 20px 48px;
    }

    .fiction-header { margin-bottom: 24px; }

    .fiction-back {
      font-size: 14px;
      color: #aab3ff;
      text-decoration: none;
    }
    .fiction-back:hover { text-decoration: underline; }

    .fiction-title {
      margin: 8px 0 4px;
      font-size: 28px;
      font-weight: 650;
    }

    .fiction-meta {
      font-size: 13px;
      color: #9ca3af;
    }

    .fiction-hero {
      margin: 24px 0;
      border-radius: 16px;
      overflow: hidden;
      box-shadow: 0 22px 40px rgba(0, 0, 0, 0.55);
    }

    .fiction-hero img {
      display: block;
      width: 100%;
    }

    .fiction-body {
      line-height: 1.8;
      font-size: 16px;
      letter-spacing: 0.02em;
      padding: 20px 18px;
      border-radius: 18px;
      background: rgba(9, 12, 28, 0.88);
      backdrop-filter: blur(14px);
      box-shadow: 0 18px 40px rgba(0, 0, 0, 0.55);
    }

    .fiction-body p { margin: 0 0 1em; }
    .fiction-body p:last-child { margin-bottom: 0; }

    .fiction-audio {
      margin-top: 24px;
      padding: 14px 18px;
      border-radius: 16px;
      background: rgba(15, 23, 42, 0.9);
      display: flex;
      flex-direction: column;
      gap: 8px;
    }

    .fiction-audio-label {
      font-size: 14px;
      color: #cbd5f5;
    }

    audio { width: 100%; }
  </style>
</head>
<body>
  <main class="fiction-page">
    <header class="fiction-header">
      <a href="/fiction/" class="fiction-back">← 返回小说列表</a>
      <h1 class="fiction-title">{{ title }}</h1>
      <p class="fiction-meta">
        {{ page.date | date("yyyy-MM-dd") }}
      </p>
    </header>

    {# 封面图片：如果 front matter 里有 cover_image，就显示你在 Admin 里选的那张 #}
    {% if cover_image %}
      <figure class="fiction-hero">
        <img src="{{ cover_image }}" alt="{{ title }}">
      </figure>
    {% endif %}

    <section class="fiction-body">
      {{ content | safe }}
    </section>

    {# 背景音乐：如果有 bgm，就渲染一个播放器 #}
    {% if bgm %}
      <section class="fiction-audio">
        <div class="fiction-audio-label">背景音乐</div>
        <audio controls preload="none">
          <source src="{{ bgm }}" type="audio/mpeg">
          你的浏览器不支持音频播放。
        </audio>
      </section>
    {% endif %}
  </main>
</body>
</html>
