<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="utf-8">
  <title>{{ title }}</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <style>
    :root {
      color-scheme: dark;
    }

    * {
      box-sizing: border-box;
    }

    body {
      margin: 0;
      /* 文学感：优先用衬线体，逐层降级 */
      font-family: "Noto Serif SC", "Source Han Serif SC", "Songti SC",
        "STSong", "SimSun", serif;
      background: radial-gradient(circle at top, #111827 0, #020617 60%);
      color: #e5e7eb;
    }

    .fiction-page {
      min-height: 100vh;
      max-width: 720px;
      margin: 0 auto;
      padding: 56px 24px 80px;
    }

    .fiction-header {
      margin-bottom: 28px;
    }

    .fiction-back {
      font-size: 13px;
      color: #a5b4fc;
      text-decoration: none;
    }

    .fiction-back:hover {
      text-decoration: underline;
    }

    .fiction-title {
      margin: 10px 0 4px;
      font-size: 30px;
      font-weight: 600;
      letter-spacing: 0.06em;
    }

    .fiction-meta {
      font-size: 12px;
      color: #9ca3af;
      text-transform: uppercase;
      letter-spacing: 0.18em;
    }

    /* 简介 / 摘要，当作 tagline 用 */
    .fiction-tagline {
      margin-top: 10px;
      font-size: 14px;
      color: #cbd5f5;
      opacity: 0.9;
      font-style: italic;
    }

    /* 正文排版：小说风格 */
    .fiction-body {
      margin-top: 40px;
      font-size: 17px;
      line-height: 1.9;
      letter-spacing: 0.03em;
      text-align: justify;
      text-justify: inter-ideograph;
    }

    .fiction-body p {
      margin: 0 0 1.3em;
      text-indent: 2em;
    }

    /* 第一段不缩进 */
    .fiction-body p:first-child {
      text-indent: 0;
    }

    /* 轻微文字光晕，让白字更柔和 */
    .fiction-body,
    .fiction-title {
      text-shadow: 0 0 6px rgba(15, 23, 42, 0.9);
    }

    /* —— 场景块组件 —— */

    .scene-block {
      margin-top: 32px;
      margin-bottom: 32px;
      padding: 20px;
      border-radius: 20px;
      background: radial-gradient(
        circle at top left,
        rgba(148, 163, 253, 0.18),
        rgba(15, 23, 42, 0.97)
      );
      display: grid;
      grid-template-columns: minmax(0, 1.2fr) minmax(0, 2fr);
      gap: 20px;
      align-items: stretch;
      box-shadow: 0 18px 40px rgba(0, 0, 0, 0.65);
    }

    @media (max-width: 768px) {
      .scene-block {
        grid-template-columns: minmax(0, 1fr);
      }
    }

    .scene-media img {
      width: 100%;
      height: 100%;
      object-fit: cover;
      border-radius: 16px;
      display: block;
      background: #020617;
    }

    .scene-text {
      display: flex;
      flex-direction: column;
      gap: 12px;
    }

    .scene-tag {
      display: inline-flex;
      align-self: flex-start;
      padding: 4px 10px;
      border-radius: 999px;
      font-size: 11px;
      letter-spacing: 0.12em;
      text-transform: uppercase;
      background: rgba(148, 163, 253, 0.2);
      color: #c7d2fe;
    }

    .scene-body {
      font-size: 15px;
      line-height: 1.8;
      color: #e5e7eb;
    }

    .scene-body p {
      margin: 0 0 0.7em;
      text-indent: 0; /* 场景说明不缩进 */
    }

    .scene-audio {
      margin-top: 10px;
      padding: 10px 12px;
      border-radius: 14px;
      background: rgba(15, 23, 42, 0.9);
    }

    .scene-audio-label {
      font-size: 12px;
      color: #9ca3af;
      margin-bottom: 6px;
    }

    .scene-audio audio {
      width: 100%;
    }

    /* 还是保留全局 audio 的样式，给结尾 BGM 用 */
    .fiction-audio {
      margin-top: 32px;
      padding: 14px 0;
    }

    .fiction-audio-label {
      font-size: 14px;
      color: #cbd5f5;
      margin-bottom: 6px;
    }

    .fiction-audio audio {
      width: 100%;
    }
  </style>
</head>
<body>
  <main class="fiction-page">
    <header class="fiction-header">
      <a href="/fiction/" class="fiction-back">← 返回小说列表</a>
      <h1 class="fiction-title">{{ title }}</h1>
      <p class="fiction-meta">
        FICTION · {{ page.date | date("yyyy-MM-dd") }}
      </p>
      {% if excerpt %}
        <p class="fiction-tagline">{{ excerpt }}</p>
      {% endif %}
    </header>

    {# 封面图片：如果有 cover，就显示 Admin 里选的那张 #}
    {% if cover %}
      <figure class="fiction-hero">
        <img src="{{ cover }}" alt="{{ title }} 封面图">
      </figure>
    {% endif %}

    <section class="fiction-body">
      {{ content | safe }}
    </section>

    {# 全局背景音乐：如果有 audio，就渲染一个播放器 #}
    {% if audio %}
      <section class="fiction-audio">
        <div class="fiction-audio-label">背景音乐</div>
        <audio controls preload="none">
          <source src="{{ audio }}" type="audio/mpeg">
          你的浏览器不支持音频播放。
        </audio>
      </section>
    {% endif %}
  </main>
</body>
</html>
