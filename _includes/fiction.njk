<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="utf-8" />
  <title>{{ title }}</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />

  <style>
    :root {
      color-scheme: dark;
    }

    * {
      box-sizing: border-box;
    }

    body {
      margin: 0;
      font-family: "Noto Serif SC", "Source Han Serif SC", "Songti SC",
        -apple-system, BlinkMacSystemFont, system-ui, "SF Pro Text", "Segoe UI",
        sans-serif;
      background:
        radial-gradient(circle at 20% 0%, rgba(129, 140, 248, 0.24) 0, transparent 55%),
        radial-gradient(circle at 80% 100%, rgba(56, 189, 248, 0.18) 0, transparent 60%),
        radial-gradient(circle at top, #111827 0, #020617 55%);
      color: #f5f5f5;
    }

    .page-fade {
      animation: pageFadeIn 650ms ease-out both;
    }

    @keyframes pageFadeIn {
      from {
        opacity: 0;
        transform: translateY(8px);
        filter: blur(2px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
        filter: blur(0);
      }
    }

    .fiction-page {
      min-height: 100vh;
      max-width: 880px;
      margin: 0 auto;
      padding: 32px 20px 64px;
    }

    .fiction-header {
      margin-bottom: 18px;
    }

    .fiction-back {
      font-size: 14px;
      color: #aab3ff;
      text-decoration: none;
    }

    .fiction-back:hover {
      text-decoration: underline;
    }

    .fiction-title {
      margin: 10px 0 6px;
      font-size: 30px;
      font-weight: 650;
      letter-spacing: 0.03em;
    }

    .fiction-meta {
      font-size: 13px;
      color: #9ca3af;
    }

    .fiction-hero {
      margin: 26px 0 18px;
      border-radius: 18px;
      overflow: hidden;
      box-shadow: 0 22px 40px rgba(0, 0, 0, 0.55);
    }

    .fiction-hero img {
      display: block;
      width: 100%;
      height: auto;
    }

    .fiction-body {
      margin-top: 24px;
      max-width: 720px;
      font-family:
        "Songti SC",
        "STSong",
        "SimSun",
        "Noto Serif CJK SC",
        "Source Han Serif SC",
        "Times New Roman",
        serif;
      font-size: 17px;
      line-height: 1.9;
      letter-spacing: 0.02em;
    }

    .fiction-body p {
      margin: 0 0 1.2em;
      text-indent: 2em;
    }

    .fiction-body p:last-child {
      margin-bottom: 0;
    }

    .fiction-body ul,
    .fiction-body ol,
    .fiction-body blockquote {
      margin: 0 0 1.2em 0;
      padding-left: 2em;
      text-indent: 0;
    }

    /* 场景块样式（图片 + 文本） */
    .story-scene {
      margin: 40px 0;
      padding: 20px;
      border-radius: 20px;
      background: radial-gradient(
        circle at top left,
        rgba(148, 163, 253, 0.16),
        rgba(15, 23, 42, 0.95)
      );
      display: grid;
      grid-template-columns: minmax(0, 1.2fr) minmax(0, 2fr);
      gap: 20px;
      align-items: stretch;
      box-shadow: 0 18px 40px rgba(0, 0, 0, 0.55);
    }

    @media (max-width: 768px) {
      .story-scene {
        grid-template-columns: minmax(0, 1fr);
      }
    }

    .scene-label {
      display: inline-flex;
      align-items: center;
      padding: 4px 12px;
      border-radius: 999px;
      font-size: 11px;
      letter-spacing: 0.12em;
      text-transform: uppercase;
      background: rgba(148, 163, 253, 0.18);
      color: #c7d2fe;
      margin-bottom: 12px;
    }

    .scene-media img {
      width: 100%;
      height: 100%;
      object-fit: cover;
      border-radius: 16px;
      display: block;
      background: #020617;
    }

    .scene-text {
      display: flex;
      flex-direction: column;
      gap: 12px;
    }

    .scene-body {
      font-size: 15px;
      line-height: 1.8;
      color: #e5e7eb;
    }

    .scene-body p {
      margin: 0 0 0.7em;
      text-indent: 0;
    }

    .scene-body p:last-child {
      margin-bottom: 0;
    }

    .audio-indicator {
      font-size: 12px;
      color: #a5b4fc;
      opacity: 0.9;
    }

    /* 顶部的「场景 BGM 同意卡片」 */
    .bgm-consent {
      margin: 10px 0 24px;
      padding: 14px 16px;
      border-radius: 16px;
      max-width: 720px;
      font-size: 14px;
      color: #e5e7eb;
      background:
        radial-gradient(circle at top left, rgba(148, 163, 253, 0.18), rgba(15, 23, 42, 0.96));
      border: 1px solid rgba(129, 140, 248, 0.5);
      box-shadow: 0 18px 30px rgba(15, 23, 42, 0.85);
    }

    .bgm-consent-text {
      margin: 0 0 10px;
      line-height: 1.7;
    }

    .bgm-consent-btn {
      border: none;
      border-radius: 999px;
      padding: 6px 14px;
      font-size: 13px;
      cursor: pointer;
      background: rgba(129, 140, 248, 0.95);
      color: #0b1020;
      font-weight: 500;
      display: inline-flex;
      align-items: center;
      gap: 6px;
      box-shadow: 0 8px 24px rgba(15, 23, 42, 0.8);
    }

    .bgm-consent-btn:hover {
      background: rgba(99, 102, 241, 1);
    }

    .bgm-consent.hidden {
      display: none;
    }
  </style>
</head>
<body>
  <main class="fiction-page page-fade">
    <header class="fiction-header">
      <a href="/fiction/" class="fiction-back">← 返回小说列表</a>
      <h1 class="fiction-title">{{ title }}</h1>
      <p class="fiction-meta">
        {{ page.date | date("yyyy-MM-dd") }}
      </p>
    </header>

    {% if cover %}
    <figure class="fiction-hero">
      <img src="{{ cover }}" alt="{{ title }} 封面图" />
    </figure>
    {% endif %}

    <!-- 场景 BGM 同意卡片 -->
    <section class="bgm-consent" id="bgm-consent">
      <p class="bgm-consent-text">
        本篇故事中的某些段落会配有背景音乐。<br />
        点击下方按钮，即视为你同意在阅读过程中这些场景可自动播放 BGM。
      </p>
      <button type="button" class="bgm-consent-btn" id="bgm-consent-btn">
        ✔ 开启场景背景音乐
      </button>
    </section>

    <!-- 正文内容（含你的 story-scene 区块） -->
    <section class="fiction-body">
      {{ content | safe }}
    </section>
  </main>

  <script>
    (function () {
      const scenes = document.querySelectorAll(".story-scene[data-audio]");
      if (!scenes.length) return;

      const audio = new Audio();
      audio.loop = true;
      let currentSrc = null;
      let userAllowed = false;

      const consentBox = document.getElementById("bgm-consent");
      const consentBtn = document.getElementById("bgm-consent-btn");

      function getMostVisibleScene() {
        let best = null;
        let bestRatio = 0;
        const vh = window.innerHeight || document.documentElement.clientHeight;

        scenes.forEach((scene) => {
          const rect = scene.getBoundingClientRect();
          const visibleHeight = Math.min(rect.bottom, vh) - Math.max(rect.top, 0);
          const ratio = visibleHeight / rect.height;
          if (ratio > bestRatio) {
            bestRatio = ratio;
            best = scene;
          }
        });

        return bestRatio >= 0.6 ? best : null;
      }

      async function playForScene(scene) {
        if (!scene) return;
        const src = scene.getAttribute("data-audio");
        if (!src) return;
        if (src === currentSrc && !audio.paused) return;

        currentSrc = src;
        audio.src = src;
        try {
          await audio.play();
        } catch (e) {
          console.warn("无法播放 BGM：", e);
        }
      }

      const observer = new IntersectionObserver(
        (entries) => {
          if (!userAllowed) return;

          const scene = getMostVisibleScene();
          if (scene) {
            playForScene(scene);
          } else {
            audio.pause();
          }
        },
        {
          threshold: [0.3, 0.6, 0.9],
        }
      );

      scenes.forEach((scene) => observer.observe(scene));

      if (consentBtn) {
        consentBtn.addEventListener("click", () => {
          userAllowed = true;
          if (consentBox) {
            consentBox.classList.add("hidden");
          }

          const scene = getMostVisibleScene();
          if (scene) {
            playForScene(scene);
          }
        });
      }

      document.addEventListener("visibilitychange", () => {
        if (document.hidden) {
          audio.pause();
        }
      });
    })();
  </script>
</body>
</html>
