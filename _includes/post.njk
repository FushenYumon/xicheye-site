<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8" />
  <title>{{ title }} | Ye · Blog</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <style>
    body {
      margin: 0;
      font-family: system-ui, -apple-system, BlinkMacSystemFont, "SF Pro Text", sans-serif;
      background: #050816;
      color: #f5f5f7;
    }
    .post-container {
      max-width: 760px;
      margin: 0 auto;
      padding: 32px 20px 64px;
    }
    .back-link {
      color: #a5b4fc;
      text-decoration: none;
      font-size: 14px;
    }
    .back-link:hover {
      text-decoration: underline;
    }
    .post-meta {
      margin: 16px 0 4px;
      font-size: 13px;
      letter-spacing: 0.08em;
      text-transform: uppercase;
      color: #9ca3af;
    }
    .post-title {
      font-size: 32px;
      margin: 4px 0 24px;
    }
    .post-content p {
      line-height: 1.7;
      margin: 12px 0;
    }
  </style>
</head>
<body>
  <main class="post-container">
    <a href="/blog/" class="back-link">← 返回 Blog</a>
    <p class="post-meta">{{ date | date("yyyy-MM-dd") }}</p>
    <h1 class="post-title">{{ title }}</h1>
    <article class="post-content">
      {{ content | safe }}
    </article>
  </main>
</body>
</html>
